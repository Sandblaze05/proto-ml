import Link from 'next/link'
import DocsLayout from '@/components/docs/DocsLayout'
import { DocsArticle, DocsAside, Note } from '@/components/docs/DocsArticle'

const toc = [{ id: 'when-to-save', label: 'When to save' }, { id: 'compare-changes', label: 'Compare changes' }, { id: 'restore-a-checkpoint', label: 'Restore a checkpoint' }, { id: 'a-simple-rhythm', label: 'A simple rhythm' }]

export default function VersionControlPage() {
  return <DocsLayout currentPath="/docs/version-control"><div className="docs-content-grid"><DocsArticle current="Version control" eyebrow="Ship with confidence" title="Give every experiment a checkpoint." description="Save meaningful graph changes, compare them clearly, and return to a known-good pipeline when an experiment goes too far." toc={toc}>
    <section id="when-to-save"><h2>When to save</h2><p>Create a version when the graph reaches a state you would be comfortable sharing: a cleaned dataset, a validated split, a baseline model, or a run with useful metrics.</p><p>Use names that describe the decision, not just the date. <strong>Baseline with deduplicated tickets</strong> is more useful than <strong>Version 4</strong> six weeks later.</p></section>
    <section id="compare-changes"><h2>Compare changes</h2><p>Before changing a project, save the current graph. After the experiment, compare the node settings, connections, and run outputs to see which decision affected the result.</p><div className="docs-table"><div><strong>Change</strong><strong>Why it matters</strong></div><div><span>Dataset</span><span>Changes the population and the meaning of every metric.</span></div><div><span>Transform</span><span>Changes feature values or the shape of downstream data.</span></div><div><span>Connection</span><span>Changes the dependency path and execution order.</span></div><div><span>Runtime</span><span>Changes where and how the graph is executed.</span></div></div></section>
    <section id="restore-a-checkpoint"><h2>Restore a checkpoint</h2><p>If a branch of experimentation is no longer useful, restore the last version that represents the direction you want to keep. Treat the restored version as a new working draft and save another checkpoint before the next major change.</p><Note>Restoring a version is a decision about the graph, not a deletion of the work you explored. Keep an experiment note in the version name or project description so the team knows why you returned.</Note></section>
    <section id="a-simple-rhythm"><h2>A simple rhythm</h2><ul className="docs-list"><li>Save before changing data preparation or model inputs.</li><li>Preview the changed branch and record what you observed.</li><li>Run the full graph once the preview is trustworthy.</li><li>Save again with a name that captures the new result.</li></ul><p>Once a version is ready to share outside the canvas, continue to <Link href="/docs/exporting">Exporting pipelines</Link>.</p></section>
  </DocsArticle><DocsAside toc={toc} /></div></DocsLayout>
}
