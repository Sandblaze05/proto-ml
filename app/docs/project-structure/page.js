import Link from 'next/link'
import DocsLayout from '@/components/docs/DocsLayout'
import { CodeBlock, DocsArticle, DocsAside, Note } from '@/components/docs/DocsArticle'

const toc = [{ id: 'the-project', label: 'The project' }, { id: 'inside-the-graph', label: 'Inside the graph' }, { id: 'designing-for-change', label: 'Designing for change' }, { id: 'versions-and-exports', label: 'Versions and exports' }]

export default function ProjectStructurePage() {
  return <DocsLayout currentPath="/docs/project-structure"><div className="docs-content-grid"><DocsArticle current="Project structure" eyebrow="Start here" title="A project is a graph you can explain." description="Keep data, transformations, and execution steps together so every result has a clear path back to its inputs." toc={toc}>
    <section id="the-project"><h2>The project</h2><p>A proto-ML project is the durable home for one machine learning workflow. It stores the canvas layout, node configuration, connections, run history, and the versions you choose to keep.</p><p>Use one project for one outcome: for example, <strong>Support ticket routing</strong> or <strong>Churn prediction</strong>. That keeps the graph legible and makes it easier for someone else to understand what a run is meant to produce.</p><CodeBlock label="project checklist">{'Project: Support ticket routing\n\nInput       → support_tickets.csv\nPreparation → Clean text → Split labels\nModel       → Classifier\nOutput      → Routing score + evaluation report'}</CodeBlock></section>
    <section id="inside-the-graph"><h2>Inside the graph</h2><p>Every graph is made from three primitives:</p><div className="docs-table"><div><strong>Primitive</strong><strong>Role</strong></div><div><span>Nodes</span><span>Declare a dataset, transform, model, or lifecycle step.</span></div><div><span>Edges</span><span>Describe which output feeds the next compatible input.</span></div><div><span>Artifacts</span><span>Capture previews, metrics, and generated outputs from a run.</span></div></div><Note>Prefer small, named steps over one large custom node. A graph is most useful when each connection explains a decision.</Note></section>
    <section id="designing-for-change"><h2>Designing for change</h2><p>Make the stable parts of a workflow easy to find and the experimental parts easy to replace. Keep raw data at the edge of the graph, normalize it once, and branch only after the shared preparation steps are complete.</p><ul className="docs-list"><li>Name nodes after the result they produce, not the implementation behind them.</li><li>Keep credentials in connected runtimes or environment settings, never in a graph export.</li><li>Save a version before changing a dataset, split strategy, or model configuration.</li></ul><p>These habits make previews easier to interpret and reduce the cost of comparing two runs.</p></section>
    <section id="versions-and-exports"><h2>Versions and exports</h2><p>Save a version when the graph reaches a meaningful checkpoint, such as a cleaned dataset or a validated model configuration. Versions preserve the graph and its settings without interrupting your working draft.</p><p>When the workflow is ready to leave the canvas, use <Link href="/docs/exporting">Exporting pipelines</Link> to generate a portable Python representation.</p><div className="docs-next-card"><div><span>Recommended next</span><strong>Learn how to arrange a readable graph</strong></div><Link href="/docs/canvas-basics">Canvas basics →</Link></div></section>
  </DocsArticle><DocsAside toc={toc} /></div></DocsLayout>
}
