import DocsLayout from '@/components/docs/DocsLayout'
import { CodeBlock, DocsArticle, DocsAside, Note } from '@/components/docs/DocsArticle'

const toc = [{ id: 'what-export-includes', label: 'What export includes' }, { id: 'review-before-export', label: 'Review before export' }, { id: 'portable-by-default', label: 'Portable by default' }, { id: 'after-the-export', label: 'After the export' }]

export default function ExportingPage() {
  return <DocsLayout currentPath="/docs/exporting"><div className="docs-content-grid"><DocsArticle current="Exporting pipelines" eyebrow="Ship with confidence" title="Take the graph with you." description="Turn a visual pipeline into a reviewable Python project without losing the structure that made it understandable." toc={toc}>
    <section id="what-export-includes"><h2>What export includes</h2><p>An export translates the graph into a portable project: dataset setup, transformation steps, execution order, and the configuration needed to reproduce the workflow.</p><CodeBlock label="export shape">{'project/\n├── pipeline.py\n├── nodes/\n├── data/\n└── README.md'}</CodeBlock><p>The exact files depend on the nodes in your graph. Keep the generated project under version control and review it like any other code change.</p></section>
    <section id="review-before-export"><h2>Review before export</h2><p>Run a final preview and full execution before exporting. Remove unused nodes, give custom steps meaningful names, and confirm that every external connection has a documented runtime or environment variable.</p><div className="docs-table"><div><strong>Check</strong><strong>Ready when</strong></div><div><span>Inputs</span><span>Paths and sources are reproducible on the target machine.</span></div><div><span>Secrets</span><span>Credentials are referenced by environment, never embedded.</span></div><div><span>Outputs</span><span>Artifacts and metrics have clear names and destinations.</span></div></div></section>
    <section id="portable-by-default"><h2>Portable by default</h2><p>Prefer standard Python data structures and explicit configuration at the edges of the graph. This keeps generated code easier to test locally and easier to move into a scheduled or remote runtime.</p><Note>Exported code is a starting point for review and integration. Add tests, error handling, and deployment configuration before treating it as production service code.</Note></section>
    <section id="after-the-export"><h2>After the export</h2><p>Open the generated README first, then run the project from a clean environment. Compare its output with the saved proto-ML run before connecting it to a larger application or scheduled job.</p><p>Keep the original graph as the visual source of truth for future edits, and export a new revision when the workflow changes.</p></section>
  </DocsArticle><DocsAside toc={toc} /></div></DocsLayout>
}
