import DocsLayout from '@/components/docs/DocsLayout'
import { CodeBlock, DocsArticle, DocsAside, Note } from '@/components/docs/DocsArticle'

const toc = [{ id: 'place-nodes', label: 'Place nodes' }, { id: 'connect-ports', label: 'Connect ports' }, { id: 'preview-a-branch', label: 'Preview a branch' }, { id: 'keep-it-readable', label: 'Keep it readable' }]

export default function CanvasBasicsPage() {
  return <DocsLayout currentPath="/docs/canvas-basics"><div className="docs-content-grid"><DocsArticle current="Canvas basics" eyebrow="Build pipelines" title="Turn a workflow into a clear canvas." description="Compose a pipeline visually, validate connections as you go, and leave enough structure for the next person to follow." toc={toc}>
    <section id="place-nodes"><h2>Place nodes</h2><p>Open the node palette and drag a node onto an open area of the canvas. Start with the source on the left, place transforms in the middle, and keep outputs or lifecycle steps on the right.</p><p>Each node has a focused job. Give it a useful name when the default label is not enough; names appear in previews, run diagnostics, and exports.</p><CodeBlock label="a readable layout">{'[Dataset] → [Clean text] → [Split]\n                              ├→ [Train]\n                              └→ [Validate]'}</CodeBlock></section>
    <section id="connect-ports"><h2>Connect ports</h2><p>Drag from an output port to a compatible input port. The canvas checks the data contract at connection time, so a text dataset cannot accidentally feed an image-only transform.</p><div className="docs-api-card"><div><span>Source output</span><code>Dataset[Row]</code></div><div><span>Transform input</span><code>Dataset[Row]</code></div><div><span>Validation</span><code>compatible</code></div></div><Note tone="warm">If a connection is rejected, inspect the port types before changing the graph. The error usually points to the first incompatible step.</Note></section>
    <section id="preview-a-branch"><h2>Preview a branch</h2><p>You do not need to run the entire graph to answer a small question. Select the branch you are working on and run a preview to inspect the rows, schema, or generated values at that point.</p><p>A useful preview check has three parts:</p><div className="docs-steps"><div><b>01</b><span>Check schema</span></div><i>→</i><div><b>02</b><span>Check samples</span></div><i>→</i><div><b>03</b><span>Check output</span></div></div><p>If the output looks wrong, fix the earliest node that introduces the problem. Downstream nodes should stay simple enough to rerun quickly.</p></section>
    <section id="keep-it-readable"><h2>Keep it readable</h2><p>Use a consistent left-to-right flow, leave room between branches, and group related steps visually. Avoid crossing edges when a small rearrangement can make the dependency obvious.</p><ul className="docs-list"><li>Keep dataset nodes together at the start of the graph.</li><li>Place reusable transforms before branching into training and validation.</li><li>Use preview runs to check each section before running the full graph.</li><li>Align related nodes and leave space for labels and diagnostics.</li></ul><div className="docs-next-card"><div><span>Next step</span><strong>Build your first pipeline</strong></div><a href="/docs/getting-started">Read quickstart →</a></div></section>
  </DocsArticle><DocsAside toc={toc} /></div></DocsLayout>
}
